language: java

java: 1.8

services:
  - #any supported service

env:
  - #env1=foo
  - #env2=bar

build:
  pre_ci:
    - echo "PRE CI"
  pre_ci_boot:
    image_name:
    image_tag:
    pull:
    options:
  ci:
    - mvn clean
    - mvn build
    - mvn package
  post_ci:
    - echo "POST CI"
  on_success:
    - echo "Success"
  on_failure:
    - echo "Failed"
  cache:
  cache_dir_list:
    - #dir1
  push:
    - #command1
