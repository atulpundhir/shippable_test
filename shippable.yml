language: java
jdk:oraclejdk7

build:
  pre_ci:
    - echo "PRE CI"
  ci:
    - mvn clean
    - mvn build
    - mvn package
  post_ci:
    - echo "POST CI"
  on_success:
    - echo "Success"
  on_failure:
    - echo "Failed"
